# アプリ名
spring.application.name=M-Chat

# データベース設定（MySQLを使用）
spring.datasource.url=jdbc:mysql://localhost:3306/m_chat?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=takashi5616
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA（自動テーブル作成）
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# コンソールでSQLを見やすくする（オプション）
spring.jpa.properties.hibernate.format_sql=true
